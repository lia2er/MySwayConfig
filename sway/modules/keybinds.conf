## Defining stuff
set $mod Mod4

set $term foot
set $menu wofi -i --show drun
set $wmenu wmenu-run -i -l 10 \
    -N 1d2021 -n bdae93 -M 282828 -m ebdbb2 -S 282828 -s ebdbb2 \
    -f "monospace 14" -p "run:"
set $log wlogout
set $swaync swaync-client -t -sw
set $polkit /usr/lib/soteria-polkit/soteria
set $file thunar
set $nightlight  gammastep -l 50.45:30.52 \
    -b 1:0.5 -t 6500K:4500K
# Gesture bindings
bindgesture swipe:3:right workspace prev
bindgesture swipe:3:left workspace next

# Key bindings
bindsym --to-code {
    # Apps shortcuts
    $mod+t exec $term
    $mod+Shift+q kill
    $mod+Shift+w exec $menu
    $mod+Shift+Control+w exec $wmenu 
    $mod+Shift+Escape exec $log
    $mod+Shift+n exec $swaync

    # Screenshot tool
    # Print exec grim

    # Windows navigation
    $mod+Left focus left
    $mod+Down focus down
    $mod+Up focus up
    $mod+Right focus right

    $mod+Shift+Left move left
    $mod+Shift+Down move down
    $mod+Shift+Up move up
    $mod+Shift+Right move right

    # Workspace switching
    $mod+1 workspace number 1
    $mod+2 workspace number 2
    $mod+3 workspace number 3
    $mod+4 workspace number 4
    $mod+5 workspace number 5
    $mod+6 workspace number 6
    $mod+7 workspace number 7
    $mod+8 workspace number 8
    $mod+9 workspace number 9

    $mod+Shift+1 move container to workspace number 1
    $mod+Shift+2 move container to workspace number 2
    $mod+Shift+3 move container to workspace number 3
    $mod+Shift+4 move container to workspace number 4
    $mod+Shift+5 move container to workspace number 5
    $mod+Shift+6 move container to workspace number 6
    $mod+Shift+7 move container to workspace number 7
    $mod+Shift+8 move container to workspace number 8
    $mod+Shift+9 move container to workspace number 9
    
    # Splits
    #$mod+h splith
    #$mod+v splitv

    # Something?
    #$mod+Shift+s layout stacking
    #$mod+Shift+w layout tabbed
    $mod+Shift+e layout toggle split

    $mod+Shift+f fullscreen

    $mod+Shift+s floating toggle
    $mod+Alt+s focus mode_toggle

    $mod+a focus parent

    $mod+Shift+minus move scratchpad
    $mod+minus scratchpad show
    
    $mod+Alt+r mode "resize"
    
    # Audio and brightness control
    --locked XF86AudioMute exec wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
    --locked XF86AudioLowerVolume exec wpctl set-volume -l 1.0 @DEFAULT_AUDIO_SINK@ 1%-
    --locked XF86AudioRaiseVolume exec wpctl set-volume -l 1.0 @DEFAULT_AUDIO_SINK@ 1%+

    --locked XF86AudioMicMute exec wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
    --locked XF86MonBrightnessDown exec brightnessctl set 1%-
    --locked XF86MonBrightnessUp exec brightnessctl set 1%+

}

mode "resize" {
    bindsym Left resize shrink width 10px
    bindsym Down resize grow height 10px
    bindsym Up resize shrink height 10px
    bindsym Right resize grow width 10px

    bindsym Escape mode "default"
}
